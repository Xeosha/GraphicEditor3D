class PointRegime {
  constructor(radius, fill, strokeWidth, stroke) {
    this.radius = radius;
    this.fill = fill;
    this.strokeWidth = strokeWidth;
    this.stroke = stroke;
  }
}

export default PointRegime;
